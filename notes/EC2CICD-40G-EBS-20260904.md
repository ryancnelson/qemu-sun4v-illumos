# ec2cicd additional unformatted EBS volume

Ryan requested creation and attachment of a new 40 GB volume and explicitly
instructed not to format it. Completed through the AWS CLI at
2026-09-04T23:29:23Z.

- Verified instance: `i-0f3608bc9d120b043`, private IP `10.215.10.217`,
  availability zone `us-east-2b`. The live host's
  `/var/lib/cloud/data/instance-id` matched AWS.
- Volume: `vol-071a1a184535ae7aa`, 40 GiB, encrypted gp3, 3000 IOPS,
  125 MiB/s throughput, name `ec2cicd-data-40g`.
- AWS attachment: `/dev/sdf`, state `attached`, volume state `in-use`,
  `DeleteOnTermination=false`.
- Current Linux device: `/dev/nvme1n1`, serial `vol071a1a184535ae7aa`.
- Stable device link:
  `/dev/disk/by-id/nvme-Amazon_Elastic_Block_Store_vol071a1a184535ae7aa`.
- No partitions, filesystem, or mount shown by `lsblk`. No formatting,
  partitioning, mount, or fstab change was performed.

Creation used an idempotency token to prevent an ambiguous retry from creating
another volume:

```sh
aws ec2 create-volume --region us-east-2 --availability-zone us-east-2b \
  --size 40 --volume-type gp3 --encrypted \
  --client-token codex-ec2cicd-40g-20260904-01a050c8 \
  --tag-specifications 'ResourceType=volume,Tags=[{Key=Name,Value=ec2cicd-data-40g},{Key=Purpose,Value=niagara-build-storage},{Key=CreatedBy,Value=codex}]'
aws ec2 attach-volume --region us-east-2 \
  --volume-id vol-071a1a184535ae7aa \
  --instance-id i-0f3608bc9d120b043 --device /dev/sdf
```

The original 80 GiB root volume `vol-05cda186b8fcee30c` was not modified.
Any future formatting or use of the new device needs Ryan's direction.
