# Drafts

Unpublished drafts kept in the repository so they can be reviewed and commented
on. Nothing here is live, and nothing here should be treated as a project claim
until it is reviewed and promoted.

## `sparc64-lives-part2-draft.html`

Draft of a follow-up page to <https://ryan.net/sparc64-lives/>, covering the work
after the original five-day account: the CI-gated multi-architecture container
release, Machine Description driven automatic boot, the hSIMD large-I/O
constraint, and the current honest list of unfinished work.

Status: **draft, not published.** It carries a visible draft banner,
`robots: noindex,nofollow`, and a trailing `DRAFT NOTES` section recording which
claims were verified live versus taken from repository evidence.

It is a single self-contained HTML file with no external CSS, JavaScript, fonts,
or images, so it can be opened or read anywhere. The palette is copied from the
live part-one page, which is a built Vite application; this file therefore does
not inherit future style changes to that site.

Open review questions are listed in the draft's own closing section.
